<% include layouts/head %>

<!-- Main div -->
<div class="grid main">

	<!-- Section title -->
	<h1>Upload and Run</h1>

	<% if(error !== '') { %>
		<div class="alert alert-red"><%= error %></div><br>
	<% } %>

	<!-- Description -->
	<p>Here you can create a new project by uploading your data files. You can test the tool by uploading our
	<a href="https://goo.gl/lrq7XR" target="_blank">sample data</a>.</p>

	<p>Please note that <b>every project is available up to 60 days</b>. After this time, the data in our server
	will be removed, and you must upload again your data if you want to continue using the tool.</p>

	<!-- Form -->
	<form action="/upload" method="post" enctype="multipart/form-data">

		<!-- Project title -->
		<h3>Title</h3>

		<!-- Description -->
		<p>You can set a title for your project. Use it for provide a short description of your experiment.</p>

		<!-- Input for email -->
		<input type="text" id="utitle" name="utitle" class="form-input form-input-500" placeholder="Yout project title">

		<!-- Spaces -->
		<br><br>

		<!-- Select files -->
		<h3>Select files</h3>

		<!-- Description -->
		<p>Press the next button for select your <b>FASTA</b> or <b>FASTQ</b> file containing the reads of your
		experiment. Additionally, if your file is too big, you can compress in a <b>zip</b> and select it.</p>

		<!-- Attach button -->
		<input type="file" id="ufile" name="ufile" class="form-input form-input-500" accept=".fasta,.fas,.fa,.fastq,.fq,.zip">

		<!-- Spaces -->
		<br><br>

		<!-- Aligner -->
		<h3>Select aligner</h3>

		<!-- Description -->
		<p>Select the aligner tool to align the reads:</p>

		<!-- Select the aligner -->
		<select class="form-select" name="ualigner" id="ualigner">
			<% for(var key in site.aligner) { %>
			<option value="<%= key %>"><%= site.aligner[key].name %></option>
			<% } %>
		</select>

		<!-- Spaces -->
		<br><br>

		<!-- Specie -->
		<h3>Select reference genome</h3>

		<!-- Description -->
		<p>Select the reference genome:</p>

		<!-- Select the aligner -->
		<select class="form-select" name="uspecie" id="uspecie">
			<% for(var key in site.specie) { %>
			<option value="<%= key %>"><%= site.specie[key].name %></option>
			<% } %>
		</select>

		<!-- Spaces -->
		<br><br>

		<!-- Set email -->
		<h3>Your email (optional)</h3>

		<!--Description -->
		<p>You can provide us an email and we will notify you when the process is completed. Also, we will provide
		you a link where you can visualize your data.</p>

		<!-- Input for email -->
		<input type="text" id="uemail" name="uemail" class="form-input form-input-500" placeholder="you@domain.com">

		<!-- Spaces -->
		<br><br>

		<!-- Run -->
		<h3>Run your experiment</h3>

		<!-- Run description -->
		<p>Check if all the information that you have provided is correct and press the next button for upload your data to the
		server and start the analysis:</p>

		<!-- Run button -->
		<div class="btn btn-blue" onclick="document.getElementById('ufile_send').click();">Upload and run</div>
		<input type="submit" value="Upload" id="ufile_send" style="display:none;">

	</form>

</div>


<% include layouts/foot %>
