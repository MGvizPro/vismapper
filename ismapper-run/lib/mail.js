//Import dependencies

//Import config
var Config = require('./../config.json');
var ISConfig = require('../../ismapper-config.json');

//Function for send the email
function Mail(id, callback)
{

	//Do the callback
	callback();
}

//Exports to node
module.exports = Mail;
